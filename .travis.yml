language:
    - c

compiler: gcc
env: CFLAGS='-Wall -Werror'

matrix:
    include:
        - compiler: gcc
          env: CFLAGS='-Wall -Werror'

before_install:
    - sudo apt-get update -qq
    - sudo apt-get install cmake

script:
    - cmake .
    - make
